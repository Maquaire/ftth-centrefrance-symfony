{% extends 'base.html.twig' %}

{% block title %}Statistiques{% endblock %}

{% block body %}
    <h1>Statistiques</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statistique.id }}</td>
            </tr>
            <tr>
                <th>Pp37</th>
                <td>{{ statistique.pp37 }}</td>
            </tr>
            <tr>
                <th>Sav37</th>
                <td>{{ statistique.sav37 }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ statistique.createdAt ? statistique.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statistiques_index') }}">back to list</a>

    <a href="{{ path('app_statistiques_edit', {'id': statistique.id}) }}">edit</a>

    {{ include('statistiques/_delete_form.html.twig') }}
{% endblock %}
